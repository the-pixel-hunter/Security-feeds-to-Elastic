# Author:
input{
  rss{
    interval => 180
    url => "url"
    tags => ["domain_tag"]
  }
}

filter{
  if ("domain_tag" in [tags]){
    mutate{
      add_field => { "[rss][source][name]" => "name" }
      add_field => { "[rss][source][name_full]" => "full_nme" }
      add_field => { "[rss][feed][type]" => "type" }
    }
  }
}