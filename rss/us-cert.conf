#Author: https://github.com/the-pixel-hunter/Secuity-RSS-Feeds
input{
	rss{
		interval => 180
		url => "https://us-cert.cisa.gov/ncas/alerts.xml"
		tags => ["us-cert"]
	}
}

filter{
if ("us-cert" in [tags]){
    mutate {
        add_field => { "source" => "US Cert" }
		add_field => { "vendor" => "None" }
		add_field => { "type" => "Advisory" }
		add_field => { "catagorys" => ["Security", "Intel"] }
		add_field => { "relevant_continent" => "America" }
        }
}
}
#Config End
